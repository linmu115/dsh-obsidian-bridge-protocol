# dsh-obsidian-bridge-protocol

Shared, versioned lifecycle messages for the Obsidian DeepHarness Bridge and DSH-side adapters. The protocol is intentionally narrow: identity, status, leases, drain, and resume. It does not expose plugin installation, unload, filesystem, or command execution.
